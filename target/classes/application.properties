spring.application.name=copiaecosurprise

server.address=0.0.0.0
server.port=8080

spring.config.import=optional:file:.env[.properties]

# Configuracion de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/ecosurprise?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Dev1045417325.

# Driver de MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracion de Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
#Puede ser update(ajusta la bd), create(borra bd y crea), create-drop(borra y crea temporal) o validate (lanza excepcion si no coincide)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

#auth0.audience=https://tu-api.com
#spring.security.oauth2.resourceserver.jwt.issuer-uri=https://TU_DOMINIO.auth0.com/


#PostgreSQL
#spring.datasource.driver-class-name=org.postgresql.Driver
#SQLServer
#spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Configuración JWT
jwt.secret=ecosurprise_secret_key_2024_secure_and_long_enough_for_hs512
jwt.expiration=86400000

# Configuración de logging
logging.level.com.itm.ecosurprise=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
